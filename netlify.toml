[context.production]
  command = "yarn build"
  publish = "dist"
  environment  = {YARN_VERSION = "1.9.4", HUGO_VERSION = "0.46"}
  
[context.deploy-preview]
  command = "yarn build-preview"
  publish = "dist"
  environment  = {YARN_VERSION = "1.9.4", HUGO_VERSION = "0.46"}

[context.process]
  command = ""
  publish = "/"
